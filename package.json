{
  "name": "jblib",
  "version": "0.0.1",
  "description": "libraries, utilities, and configs i use to make stuff",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "fmt": "prettier --check --cache .",
    "fmt:fix": "prettier --write --cache .",
    "ci": "pnpm run fmt && pnpm run lint && pnpm run test",
    "changeset": "changeset",
    "changeset:version": "changeset version && pnpm install --no-frozen-lockfile && pnpm fmt:fix",
    "changeset:publish": "changeset publish"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@arethetypeswrong/core": "catalog:",
    "@changesets/cli": "catalog:",
    "@types/node": "catalog:",
    "prettier": "catalog:",
    "publint": "catalog:",
    "eslint": "catalog:",
    "jiti": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:",
    "sherif": "catalog:",
    "jsdom": "catalog:",
    "tsdown": "catalog:"
  }
}
